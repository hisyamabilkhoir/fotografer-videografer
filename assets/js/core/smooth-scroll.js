/* ============================================
   ABYNSS STUDIO — Smooth Scroll (Lenis)
   ============================================ */

import { prefersReducedMotion } from '../utils.js';

export function initSmoothScroll() {
  // Disabled by user request to keep scrolling native, fast, and responsive.
  return null;
}
